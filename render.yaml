services:
  - type: web
    name: ethereum-event-listener
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: PORT
        value: 10000
      - key: RPC_URL
        sync: false
