services:
  - type: web
    name: ethereum-event-listener
    env: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
      - key: RPC_URL
        sync: false
